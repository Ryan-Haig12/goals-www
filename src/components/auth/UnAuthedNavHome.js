import React from 'react'
import { Redirect } from 'react-router-dom'

// this file used to be a lot cooler
// imma keep it around for now but it doesn't seem too important anymore
const UnAuthedNavHome = () => ( <Redirect to="/" /> )

export default UnAuthedNavHome
