const INITIAL_STATE = {
    token: null,
    user: null
}

const AuthReducer = (state = INITIAL_STATE, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default AuthReducer